# Local dev: leave empty (Vite proxies /api to localhost:8081)
# Production (Cloudflare): Railway API URL without trailing slash
# Example: VITE_API_BASE=https://finance-bot-api.up.railway.app
VITE_API_BASE=
